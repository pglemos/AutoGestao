// Compatibility entrypoint retained for callers of the historical filename.
// The implementation is current-snapshot based; it does not encode historical counts.
await import('./generate-security-definer-matrix.mjs')
