import { Avatar } from '@/components/atoms/Avatar'
import { ReportDataTable } from '@/features/internal-reports/ReportDataTable'
import type { RankingEntry } from '@/types/database'
export function SellerPerformanceTable({ rows, onSelect }: { rows: RankingEntry[]; onSelect: (id: string, storeId?: string) => void }) { const mapped = rows.map(entry => ({ ...entry, id: entry.user_id })); return <ReportDataTable title="Vendedores" description="Selecione um vendedor para consultar a leitura individual." rows={mapped} columns={[{ key: 'seller', label: 'Vendedor', render: row => <button type="button" className="flex items-center gap-3 text-left" onClick={() => onSelect(row.user_id, String((row as RankingEntry & { store_id?: string }).store_id || ''))}><Avatar src={row.avatar_url || undefined} fallback={row.user_name} size="sm" /><span className="font-semibold text-gray-800">{row.user_name}</span></button> }, { key: 'sales', label: 'Vendas', align: 'center', render: row => row.vnd_total }, { key: 'leads', label: 'Leads', align: 'center', render: row => row.leads }, { key: 'attainment', label: 'Atingimento', align: 'right', render: row => `${Number(row.atingimento || 0).toFixed(1)}%` }]} /> }
