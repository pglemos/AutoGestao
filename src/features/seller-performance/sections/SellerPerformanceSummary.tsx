import { Activity, Target, TrendingUp, Users } from 'lucide-react'
import { MxMetricCard, MxMetricGrid, MxSectionCard, MxSectionHeader } from '@/components/module/MxModuleVisualPrimitives'
import type { SellerPerformanceViewModel } from '../lib/sellerPerformanceViewModel'
export function SellerPerformanceSummary({ seller }: { seller: SellerPerformanceViewModel }) { return <><MxSectionCard><MxSectionHeader title={seller.name} description={seller.storeName} /><div className="p-5 text-sm text-muted-foreground">Leitura individual baseada no mesmo conjunto de dados do ranking.</div></MxSectionCard><MxMetricGrid><MxMetricCard title="Vendas" value={seller.sales} detail="Total registrado" icon={TrendingUp} tone="success" /><MxMetricCard title="Leads" value={seller.leads} detail="Entradas trabalhadas" icon={Users} /><MxMetricCard title="Conversão" value={`${seller.conversion.toFixed(1)}%`} detail={`${seller.sales} de ${seller.leads}`} icon={Activity} tone="info" /><MxMetricCard title="Atingimento" value={`${seller.attainment.toFixed(1)}%`} detail="Relação com a meta" icon={Target} tone={seller.attainment >= 100 ? 'success' : 'warning'} /></MxMetricGrid></> }
