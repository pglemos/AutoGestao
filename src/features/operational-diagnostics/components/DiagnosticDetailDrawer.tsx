import { Button } from '@/components/atoms/Button'
import type { DiagnosticFinding } from '../types'
export function DiagnosticDetailDrawer({ finding, onClose }: { finding: DiagnosticFinding | null; onClose: () => void }) { if (!finding) return null; return <div className="fixed inset-0 z-50 flex justify-end bg-black/40" role="dialog" aria-modal="true" aria-labelledby="diagnostic-detail-title" onMouseDown={event => { if (event.currentTarget === event.target) onClose() }}><aside className="h-full w-full max-w-lg overflow-y-auto bg-white p-5 shadow-xl"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-semibold text-emerald-700">Diagnóstico operacional</p><h2 id="diagnostic-detail-title" className="mt-1 text-xl font-bold text-foreground">{finding.title}</h2></div><Button variant="secondary" onClick={onClose}>Fechar</Button></div><p className="mt-5 text-sm leading-6 text-muted-foreground">{finding.description}</p><h3 className="mt-6 font-semibold text-foreground">Ação sugerida</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{finding.suggestedAction}</p><h3 className="mt-6 font-semibold text-foreground">Evidências</h3><ul className="mt-2 list-disc space-y-1 pl-5 text-sm text-muted-foreground">{finding.evidence.map(item => <li key={item}>{item}</li>)}</ul></aside></div> }
