import { MxEmptyState, MxTableSurface } from '@/components/module/MxModuleVisualPrimitives'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/organisms/Table'
import { diagnosticSeverityLabel } from '../lib/diagnosticSeverity'
import type { DiagnosticFinding } from '../types'
export function DiagnosticFindingTable({ findings, onSelect }: { findings: DiagnosticFinding[]; onSelect: (finding: DiagnosticFinding) => void }) { if (!findings.length) return <MxEmptyState title="Nenhum achado" description="Não foram identificadas inconsistências no período." />; return <MxTableSurface><Table className="min-w-[720px]"><TableHeader><TableRow><TableHead>Severidade</TableHead><TableHead>Achado</TableHead><TableHead>Evidências</TableHead><TableHead>Ação</TableHead></TableRow></TableHeader><TableBody>{findings.map(finding => <TableRow key={finding.id}><TableCell>{diagnosticSeverityLabel(finding.severity)}</TableCell><TableCell><strong className="block text-foreground">{finding.title}</strong><span className="text-muted-foreground">{finding.description}</span></TableCell><TableCell>{finding.evidence.join(' · ')}</TableCell><TableCell><button type="button" className="font-semibold text-status-success-text" onClick={() => onSelect(finding)}>Ver detalhe</button></TableCell></TableRow>)}</TableBody></Table></MxTableSurface> }
