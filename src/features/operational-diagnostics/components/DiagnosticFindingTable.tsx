import { MxEmptyState, MxTableSurface } from '@/components/module/MxModuleVisualPrimitives'
import { diagnosticSeverityLabel } from '../lib/diagnosticSeverity'
import type { DiagnosticFinding } from '../types'
export function DiagnosticFindingTable({ findings, onSelect }: { findings: DiagnosticFinding[]; onSelect: (finding: DiagnosticFinding) => void }) { if (!findings.length) return <MxEmptyState title="Nenhum achado" description="Não foram identificadas inconsistências no período." />; return <MxTableSurface><table className="min-w-[720px] w-full text-sm"><thead><tr className="border-b border-gray-100 bg-gray-50 text-left text-gray-500"><th className="px-4 py-3">Severidade</th><th className="px-4 py-3">Achado</th><th className="px-4 py-3">Evidências</th><th className="px-4 py-3">Ação</th></tr></thead><tbody>{findings.map(finding => <tr key={finding.id} className="border-b border-gray-100 last:border-0"><td className="px-4 py-4">{diagnosticSeverityLabel(finding.severity)}</td><td className="px-4 py-4"><strong className="block text-gray-800">{finding.title}</strong><span className="text-gray-500">{finding.description}</span></td><td className="px-4 py-4">{finding.evidence.join(' · ')}</td><td className="px-4 py-4"><button type="button" className="font-semibold text-emerald-700" onClick={() => onSelect(finding)}>Ver detalhe</button></td></tr>)}</tbody></table></MxTableSurface> }
