import { Download, RefreshCw } from 'lucide-react'
import { Button } from '@/components/atoms/Button'
export function ReportExportActions({ exporting, refreshing, canExport, onExport, onRefresh }: { exporting: boolean; refreshing: boolean; canExport: boolean; onExport: () => void; onRefresh: () => void }) { return <><Button variant="secondary" onClick={onRefresh} disabled={refreshing}><RefreshCw size={18} className={refreshing ? 'animate-spin motion-reduce:animate-none' : ''} />Atualizar</Button>{canExport ? <Button variant="outline" onClick={onExport} disabled={exporting}><Download size={18} />{exporting ? 'Exportando...' : 'Exportar'}</Button> : null}</> }
