/**
 * Re-export do container em `src/features/filiais/StoreBranches.container.tsx`,
 * seguindo o padrão das demais rotas (lazy-load a partir de `@/pages/*`).
 */
export { StoreBranches, StoreBranches as default } from '@/features/filiais/StoreBranches.container'
