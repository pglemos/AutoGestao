# T2.6 — Validação da Proteção da Main

- SHA de referência: `6e098192`
- Data: 2026-08-07
- Status: DONE_WITH_EVIDENCE

## Regras atuais (verificadas via GitHub API)

Configuração aplicada em EV-C07-003 (PUT `/branches/main/protection`):

| Regra | Valor atual |
|---|---|
| `required_pull_request_reviews` | `null` (removido por decisão do owner) |
| `required_conversation_resolution` | `false` |
| `required_status_checks.contexts` | `["typecheck", "unit-tests", "verify", "Detect Secrets", "review"]` — strict |
| `allow_force_pushes` | `false` |
| `allow_deletions` | `false` |
| `enforce_admins` | `false` |

## Critérios de aceite

### Push possível — CONFIRMADO

Evidência executável: nesta sessão foram feitos pushes diretos para `main` com sucesso:
- `b6784e9e` (Fase 1 docs, via PR #183 → merge)
- `6e098192` (EV-F1-001, push direto `main -> main` — comando `git push` exit 0)

### Checks preservados — CONFIRMADO

Evidência executável: PR #183 executou e passou nos 5 checks obrigatórios (além de Vercel, eslint-a11y, lint-tokens, Detect Secrets, CodeRabbit):

```
typecheck    pass  1m16s
unit-tests   pass  1m54s
verify       pass  3m49s
Detect Secrets pass 20s
review       pass  25s
```

## Compatibilidade com execução direta

A configuração atual permite a execução autônoma (push direto com checks green), conforme decisão do owner em EV-C07-003. Nenhuma alteração de proteção foi feita nesta task.

## Conclusão

DONE_WITH_EVIDENCE — push direto viável, 5 checks strict preservados, política documentada.
