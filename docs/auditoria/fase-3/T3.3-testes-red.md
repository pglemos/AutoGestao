# T3.3 — Testes Red (vercel-ignore-build)

- SHA de referência: `ec2885c2` (PR #185 merged)
- Data: 2026-08-06/07
- Status: DONE_WITH_EVIDENCE

## Testes red escritos (antes da correção)

`scripts/vercel-ignore-build.test.mjs` — 15 testes `node --test` cobrindo:

1. `isDocumentationOnly` para prefixos canônicos (`docs/`, `evidence/`, `screenshots/`, etc.)
2. `isDocumentationOnly` para extensões `.md` / `.mdx`
3. Arquivos runtime (`src/`, `.env`, `api/`, `supabase/`, `.sql`, `package.json`, etc.)
4. `classifyChangedFiles` particiona corretamente
5. `shouldSkipBuild` com docs-only → `true`
6. `shouldSkipBuild` com qualquer runtime → `false`
7. `shouldSkipBuild([])` → `true` (vazio = docs-only)
8. `detectRepoSlug` via `VERCEL_GIT_REPO_OWNER` + `VERCEL_GIT_REPO_SLUG`
9. `detectRepoSlug` com slug já completo (`owner/slug`)
10. `detectRepoSlug` fallback via remote (padrão `github.com/:owner/:repo`)
11. `detectRepoSlug` sem owner/slug/remote → `null`
12. `diffFilesLocally` com commits inexistentes → `null`
13. `main()` sem `VERCEL_GIT_PREVIOUS_SHA` → exit 1 (build)
14. `main()` com `VERCEL_GIT_PREVIOUS_SHA` zerado → exit 1 (build)
15. `main()` docs-only → exit 0 (skip)

## Critérios de aceite

- [x] Testes red falham ANTES da correção (reprodução da falha registrada)
- [x] Testes verdes DEPOIS da correção
- [x] Comando: `node --test scripts/vercel-ignore-build.test.mjs` — 15/15 pass

Conclusão: **DONE_WITH_EVIDENCE** — EV-F3-003.
