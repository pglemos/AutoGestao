# T3.7 — Validação de Preview e Produção

- SHA de referência: `571f547f` (produção atual)
- Data: 2026-08-07
- Status: DONE_WITH_EVIDENCE

## Validações executadas

### Domínios e aliases
- Apex `mxperformance.com.br` → alias Vercel (308)
- `www.mxperformance.com.br` → alias Vercel (produção ativa)
- Deploy `mxperformance-*.vercel.app` (URL canônica de deploy)

### Headers de segurança (www, produção)
| Header | Valor |
|---|---|
| `Content-Security-Policy` | completa (default-src, script-src com nonce/hash, style-src, img-src, connect-src) |
| `Strict-Transport-Security` | `max-age=63072000; includeSubDomains; preload` |
| `X-Frame-Options` | `DENY` |
| `X-Content-Type-Options` | `nosniff` |
| `Referrer-Policy` | `strict-origin-when-cross-origin` |
| `Permissions-Policy` | restritiva |

### Redirects
- `http://` → `https://` (308)
- `apex` → `www` (308)
- Cachê: `x-vercel-cache: HIT`

### Environment (production)
- `SENTRY_ORGANIZATION=synvolt`, `SENTRY_PROJECT`, `VITE_SENTRY_DSN`, `VITE_SENTRY_ENVIRONMENT=production`
- `VITE_PUBLIC_SUPABASE_ANON_KEY`, `POSTGRES_*` (pooler), etc.

### Preview
- Deploy de preview Ready; `/api/health` preview → `environment: preview`, `supabase_api: fail` (esperado — envs de preview não configuradas; comportamento documentado).

### Health check (produção)
```
{"status":"healthy","checks":{"vercel":"ok","supabase_api":"ok","database":"ok","critical_crons":"unknown"},
 "release":"571f547f3753364d13dae8acd57774e7d3a52470","environment":"production"}
```
- Antes: `database: fail` + HTTP 503 (regressão C0.5) → **EV-F3-007**: migration `20260807000000_restore_anon_execute_mx_database_health.sql` restaurou `EXECUTE` para `anon` (única RPC legítima de liveness; SECURITY DEFINER, sem exposição de dados; `REVOKE ... FROM PUBLIC` mantém exposição mínima).
- Depois: `database: ok` + HTTP 200.

### Rollback (prev == cur)
- Redeploy manual do mesmo SHA → Canceled "0 file(s)" (correto, sem rebuild desperdiçado).
- Range com runtime entre prev e cur → sempre build (garantia conservadora do script).

## Critérios de aceite

- [x] Preview aprovado
- [x] Produção aprovada (domínios, aliases, headers, redirects, envs, health)

Conclusão: **DONE_WITH_EVIDENCE** — EV-F3-007 e EV-F3-008.
