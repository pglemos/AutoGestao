# T4.1 — Auditoria de Tokens Existentes

- SHA de referência: `b67238f1`
- Data: 2026-08-07
- Status: DONE_WITH_EVIDENCE

## Objetivo

Inventariar todas as fontes de tokens (CSS, TS, Tailwind, valores hardcoded) para
escolher a fonte canônica no T4.2.

## Inventário — fontes canônicas já existentes

### Camadas CSS (3 níveis) — `src/design-system/tokens/`
| Camada | Arquivo | Tokens | Papel |
|---|---|---|---|
| 1 — Primitivas | `primitives.css` | 119 | Valores crus (cores HSL, espaço 4px, breakpoints MD3, raios, sombras, durations, easings). Nunca consumidos direto. |
| 2 — Semânticas | `semantic.css` | 103 | Intenção de uso (`--mx-color-*`, superfícies, texto). Publicadas sob `.mx-ds` + bloco `:root` de geometria. Redefine shadcn (`--primary`, `--background`…) via escopo. |
| 3 — Componentes | `components.css` | 64 | Dimensões/ritmo por família (botão, input, card…) + densidade `data-mx-density`. |

### Espelhos TS
- `src/design-system/tokens/colors.ts` — `mxColor()` via `getComputedStyle` + fallback.
- `src/design-system/tokens/index.ts` — `MX_Z_INDEX` (escala fechada §34), `MX_MOTION` (§20), `MX_DS_ROOT_CLASS`.
- `src/lib/charts/tokens.ts` — paleta de charts (recharts), runtime + fallback.

### Tailwind
- `tailwind.config.js` — **herança do template canônico**: paleta `mx` legada (navy/dark/teal/action/success/warning/danger + bg/surface/border/divider/text/muted/subtle), shadows (card/popover/modal/action), radius (md/lg/xl/2xl). **Tailwind v4** (`@tailwindcss/vite`, `@theme` em `src/index.css`) — o config v3 não é a fonte efetiva de runtime; o `@theme` mapeia `--color-mx-*` para os tokens semânticos (`hsl(var(--mx-color-*))`) e ainda contém **6 hex legados diretos** (`#071822`, `#051923`, `#031B3D`, `#0F172A`, `#22C55E`, `#F59E0B` etc.).
- `src/index.css` (1889 linhas) — `@theme` + 34 blocos de componentes globais; mantém `--color-*` shadcn e `font-family: Inter` / `ui-monospace`.

## Inventário — valores hardcoded (dívida)

### Cores hex/rgb em produção (fora de tokens)
| Tipo de arquivo | Qtde | Situação |
|---|---|---|
| `.jsx` (template canônico legado) | 64 | Não escaneados por `lint-tokens-ast` (design); são os componentes do template a migrar |
| `.tsx`/`.ts` modernos | 47 | Todos cobertos por whitelist explícita ou por `lint-tokens-ast` (881 arquivos escaneados, **OK — nenhum hex fora da whitelist**) |
| Testes/stories/base44-reference | ~80 | Referência visual / contrato de paridade — exceção intencional |

### Dimensões inline (spacing/font)
- **889 usos** de `p-[Npx]`, `m-[Npx]`, `gap-[Npx]`, `text-[Npx]` em `src/components` + `src/pages` (topo: `FunilVendedor.tsx` 69, `RegularizarFechamentoDrawer.jsx` 56, `FichaClienteSheet.jsx` 45).

### Z-index (dívida congelada)
- Escala fechada de destino: `--mx-z-*` em `components.css` (§34).
- Baseline congelada: `scripts/z-index-baseline.json` (21 valores distintos históricos; hoje 58 ocorrências monitoradas — **2 a menos** que o inventário inicial; `lint-z-index` OK).
- Usos comuns ainda arbitrários: `z-10` dominante (11+ por arquivo em StoreBattleView, LegacyMorningReportPage, etc.).

### Motion
- 5 usos de `duration-[Nms]` inline; 15 arquivos já consomem `--mx-duration`/`--mx-easing` diretamente.

## Ferramentas de guarda existentes
| Ferramenta | Comando | Status |
|---|---|---|
| `lint-tokens-ast.mjs` (AST TS, hex hardcoded) | `npm run lint:tokens` | OK — 881 arquivos |
| `lint-z-index.mjs` (baseline congelada) | `npm run lint:z-index` | OK — 58 ocorrências |
| `tokens-contract.test.ts` (confronta Base44 medido) | `npm test` | contratos verdes |
| `lint-tokens.legacy.js` | `npm run lint:tokens:legacy` | guarda legado |

## Critérios de aceite

- [x] Inventário completo: CSS (286 tokens em 3 camadas), TS (3 espelhos), Tailwind (@theme + config legado), hardcoded (113 arquivos de produção + 889 dimensões inline + z-index + motion)
- [x] Ferramentas de guarda mapeadas (4)
- [x] Evidência executável (comandos com saída OK) registrada

## Riscos residuais
- `tailwind.config.js` legado pode induzir a erro (Tailwind v4 ignora por padrão; `@theme` é a fonte real).
- 64 `.jsx` do template não são escaneados por design — migração é obrigação de outra story, mas devem ficar documentados como dívida.

Conclusão: **DONE_WITH_EVIDENCE**.
