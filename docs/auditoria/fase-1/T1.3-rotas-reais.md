# T1.3 — Inventário de Rotas Reais

- SHA de referência: `814154f8`
- Data: 2026-08-07
- Status: DONE_WITH_EVIDENCE

## Manifesto executável

O manifesto canônico é gerado por `scripts/audit_route_data_inventory.mjs` e commitado em `docs/auditoria/matrizes/MATRIZ_ROTAS_DADOS_MX.md`. O contrato de sincronia manifesto↔runtime é garantido por teste:

- `npm run audit:routes-data -- --check` → exit 0 (matriz commitada = fontes atuais)
- `bun test src/lib/route-data-inventory-contract.test.ts` → 2 pass, 0 fail

## Contagem exata

| Métrica | Valor |
|---|---|
| Rotas declaradas em `src/App.tsx` | **109** |
| Rotas protegidas | **101** |
| Rotas públicas | **8** |
| Rotas protegidas sem regra canônica e sem redirect | **0** |
| Caminhos declarados mais de uma vez | **0** |
| Regras de acesso canônicas (`ROUTE_ACCESS_RULES`) | **95** |
| Redirects/aliases (`Navigate`/`RedirectWithSearch`) | **43** |
| Tabelas referenciadas pelo runtime | **127** |
| RPCs referenciadas pelo runtime | **87** |
| Edge Functions invocadas pelo runtime | **14** |
| Catch-all (`/notfound`) | 1 (`*` em App.tsx:495) |

## Rotas públicas (8)

`/`, `/login`, `/forgot-password`, `/reset-password`, `/pre-cadastro/:storeSlug`, `/privacy`, `/terms`, `*` (NotFound — público com 404)

## Rotas parametrizadas (9)

| Rota | Parâmetro |
|---|---|
| `/pre-cadastro/:storeSlug` | slug da loja |
| `/lojas/:storeSlug` | slug da loja |
| `/lojas/:storeSlug/consultor-ia` | slug da loja |
| `/lojas/:storeSlug/equipe` | slug da loja |
| `/lojas/:storeSlug/filiais` | slug da loja |
| `/consultoria/clientes/:clientSlug` | slug do cliente |
| `/consultoria/clientes/:clientSlug/visitas/:visitNumber` | slug + número da visita |
| `/pdi/:id/print` | id do PDI |
| `/simulacao/:simulationRole` | papel simulado |

## Perfis autorizados

- Perfis internos MX: `administrador_geral`, `administrador_mx`, `consultor_mx` (herdam admin)
- Perfis de negócio: `dono`, `gerente`, `vendedor`
- Matriz por rota: `ROUTE_ACCESS_RULES` em `src/lib/auth/routeAccess.ts` (95 padrões, roles e/ou capabilities)
- `RoleSwitch` em `App.tsx:506` resolve o elemento por perfil em rotas multi-perfil

## Achados

| Achado | Classificação | Ação |
|---|---|---|
| Rota `/consultor-ia` (App.tsx:456) redireciona via alias para `/lojas/:slug/consultor-ia` | Alias legado (intencional) | Manter (compatibilidade de link) |
| `/team` e `/equipe` são aliases do mesmo destino | Alias (intencional) | Manter |
| 0 rotas protegidas sem regra canônica | OK | — |
