# T1.2 — Inventário de Scripts e Workflows

- SHA de referência: `814154f8`
- Data: 2026-08-07
- Status: DONE_WITH_EVIDENCE

## Inventário de workflows (21)

| Workflow | Triggers | Job(s) | Gate CI obrigatório? |
|---|---|---|---|
| `atomic-lint.yml` | push, PR, dispatch | lint-tokens | não (não é check-name requerido) |
| `bundle-budget.yml` | PR, dispatch | budget | não |
| `carteira-clientes-parity-verification.yml` | push, PR, dispatch | verify | não |
| `central-execucao-parity-verification.yml` | push, PR, dispatch | verify | não |
| `coderabbit-review.yml` | PR | review | **sim** (context `review`) |
| `db-types-diff.yml` | PR, dispatch | diff | não |
| `edge-functions-check.yml` | push, PR | deno-check | não |
| `eslint-a11y.yml` | push, PR, dispatch | eslint-a11y | não |
| `gitleaks.yml` | PR, push, dispatch | scan | não (context `Detect Secrets` vem de outro mecanismo) |
| `management-design-system-audit-v3.yml` | PR, push | audit | não |
| `manager-parity-verification.yml` | PR | verify | não |
| `migration-checksums.yml` | PR, dispatch | checksums | não |
| `migration-reversibility.yml` | PR | check-down-block, ephemeral-up-down-up | não |
| `module-design-system-authenticated-visual.yml` | PR | authenticated-visual | não |
| `module-design-system-parity.yml` | PR, push | parity | não |
| `quality-gates.yml` | push, PR, dispatch | verify | **sim** (context `verify`) |
| `rls-matrix.yml` | PR, push | rls-matrix | não |
| `smoke-403.yml` | push, PR, dispatch | smoke | não |
| `storybook-build.yml` | PR, push | build | não |
| `sync-carteira-db-types.yml` | PR, push | generate | não |
| `typecheck-and-unit-tests.yml` | push, PR, dispatch | typecheck, unit-tests | **sim** (contexts `typecheck`, `unit-tests`) |

Proteção de main exige 5 checks: `typecheck`, `unit-tests`, `verify`, `Detect Secrets`, `review` — todos mapeados a workflows existentes.

## Inventário de scripts npm (43 no package.json)

- **Dev/build**: dev, build, preview, clean, storybook, build-storybook
- **Qualidade**: lint (+ lint:tokens, lint:z-index, lint:page-roots, lint:landmarks, lint:a11y, lint:tokens:legacy), typecheck, test, test:e2e, test:e2e:ui, check:bundle-size
- **Auditorias**: audit:routes-data, audit:management-design-system
- **AIOX**: sync:ide, sync:ide:check, sync:skills:codex, sync:skills:codex:global, validate:structure, validate:agents, validate:parity
- **Ops/Deploy**: deploy, validate:e2e:live, validate:admin-master:e2e:live, export:team-contacts, reconcile:pre-cadastro-team, supabase:update-recovery-email, gen:db-types, verify:db-types
- **Consultoria**: consultoria:carregar-parametros, consultoria:importar-fechamento-mensal, consultoria:sincronizar-metricas-pmr, consultoria:gerar-planejamento-estrategico, consultoria:gerar-resumo-executivo
- **Órfãos (achado)**: `fix:admin-access` → `scripts/fix_admin_access.ts` **NÃO EXISTE**; `seed:sandbox:live` → `scripts/seed_live_sandbox.ts` **NÃO EXISTE**

## Scripts standalone (99 em `scripts/`)

Categorias: audit (audit-*.mjs, check_*.ts), generators de matrizes (generate-*-matrix.mjs, build-*-matrix.mjs), builders de docs (build-factual-*.mjs), utilitários de migração/checksums, captura de telas (capture_*.js), verificação Vercel/parity, smoke 403.

## Hooks locais

- Nenhum hook git local ativo (`~/.git/hooks` sem hooks custom, sem `.husky`).
- Gate real acontece nos 5 checks obrigatórios do CI + CodeRabbit.

## Achados

| Achado | Classificação | Ação |
|---|---|---|
| 2 scripts npm apontam para arquivo inexistente | Órfão (dívida) | Remover entradas do package.json OU recriar scripts (sem uso ativo) |
| Gate de CI depende de 5 checks de 4 workflows | OK | Nada a fazer |
| 17 workflows não obrigatórios | OK (guard-rails extras) | Manter |
