# T1.6 — Inventário de Dívida e Marcadores

- SHA de referência: `814154f8`
- Data: 2026-08-07
- Status: DONE_WITH_EVIDENCE
- Método: análise estática (grep + contagem) — cada achado classificado.

## 1. Marcadores em código de produção (`src/`)

| Marcador | Qtd | Classificação |
|---|---|---|
| `TODO` real | **0** | OK — zero TODO pendente em produção |
| `FIXME` | **0** | OK |
| `HACK` | **0** | OK |
| `XXX` | 0 (1 match é string `XX/XX/XXXX` em calculations.ts:329) | OK — falso positivo |
| `@ts-ignore` / `@ts-expect-error` | **0** | OK — tipagem sem escapes |
| `console.log` em runtime | **0** (7 ocorrências, todas em `src/test/**` Playwright) | OK |
| `eslint-disable` | 2 (`ModalTrigger.tsx:22`, `TimeGrid.tsx:159`) — a11y justificado e comentado | OK — exceção documentada |
| `lint-page-roots-ignore` | 10 (páginas centradas: Login, 404, Terms, Privacy, etc.) | OK — contrato de lint do projeto |

## 2. Feature flags

- `src/lib/feature-flags.ts` — flag build-time `VITE_FLAG_LANCAMENTOS_VIA_RPC` (lançamentos via RPC).
- `VITE_ENABLE_DEV_AUTH_BYPASS` — presente no runtime; deve estar desligado em produção (verificar Fase 14).

## 3. Dívida estrutural

| Item | Classificação |
|---|---|
| `supabase/migrations/00000000000000_baseline_legacy_schema.sql` (3.334 linhas, schema inteiro herdado) | RISCO ALTO — baseline monolítico, sem granularidade por domínio; requer Fase de migração/dividir em fatias |
| 346 migrations ativas + **42 arquivadas** em `supabase/migrations/_archived/` | OK — processo de arquivamento existe e é usado |
| ~~`scripts/legacy/`~~ — removidos em 2026-08-06 (scripts de auditoria antigos sem owner, não referenciados no package.json) | RESOLVIDA |
| `src/api/base44Client.js` (shim Base44 com mocks) + `base44-reference/` (195 arquivos) | OK — por design, base de paridade (v. T1.4) |
| 2 scripts npm órfãos: `fix:admin-access` e `seed:sandbox:live` (arquivos inexistentes) | DÍVIDA — corrigir em Fase 17 |
| 36 componentes JSX órfãos (0 imports) + 36 duplicações jsx↔tsx (v. T1.4) | DÍVIDA — remoção segura após cobertura de rotas |
| 89 `catch {}` sem log em `src/` | BAIXO — possível mascaramento de erros; revisar em Fase 14 |
| `docs/AUDITORIA-SISTEMA.md` (16.6KB, 2026-07-06) — laudo de auditoria anterior | OK — referência histórica; números antigos divergem da contagem atual (esperado, contagem atual é a canônica) |

## 4. Documentos de auditoria existentes (histórico)

- `docs/auditorias/` — 18 laudos (2026-06-17 a 2026-07-17): base44 vs MX, vendedor, UX, qualidade de código, homologação central carteira.
- Fase 1 atual (T1.1–T1.6) é a auditoria integral atualizada — documento canônico a partir de agora.

## 5. Riscos levantados (herdados para fases seguintes)

1. **Dívida de tipagem em `api/base44Client.js`** (JS sem types, mocks).
2. **89 catches silenciosos** — cobertura de erro incompleta.
3. **`VITE_ENABLE_DEV_AUTH_BYPASS`** em runtime — validar em produção.
4. **`scripts/legacy/` (23 arquivos)** — candidatos a remoção/arquivamento.
5. **Baseline migration monolítica (3.334 linhas)** — qualquer mudança de schema precisa de cuidado com colisões e ordem de aplicação.

## 6. Conclusão

O código de produção está **limpo de marcadores** (0 TODO/FIXME/HACK/@ts-ignore/console.log). A dívida concentra-se em: baseline migration monolítica, 23 scripts legacy órfãos, 36 JSX órfãos, 2 scripts npm órfãos e 89 catches silenciosos — todos classificados e endereçáveis nas fases curativas (Fase 14 observabilidade, Fase 17 backlog/limpeza).
