# T1.5 — Inventário de Integrações

- SHA de referência: `814154f8`
- Data: 2026-08-07
- Status: DONE_WITH_EVIDENCE

## Integrações por serviço

### 1. Supabase (core)
- **Contrato**: URL + anon key em `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` (fallback `VITE_PUBLIC_*`); singleton `src/lib/supabase.ts`; REST + Realtime (wss); RPCs (87 usadas pelo runtime); Storage.
- **Projeto**: `fbhcmzzgwjdgkctlfvbo` (org `vercel_icfg_cfbM73bXpmlSmtA5dT0EZOVG`).
- **Secrets esperados**: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (server-side), `SUPABASE_PROJECT_REF`.
- **Storage buckets**: `evidencias-consultoria` (+ outros nas migrations de storage).
- **Crons pg_cron (12)**: `cleanup-oauth-states`, `mx-consolidate-d1-0931`, `mx-critical-jobs-health`, `mx-google-meet-ata`, `mx-matinal-report`, `mx-monthly-report`, `mx-morning-report-1030`, `mx-refresh-manager-routine-snapshots`, `mx-refresh-seller-routine-snapshots`, `mx-refresh-store-target-plans`, `mx-weekly-feedback`, `mx-weekly-feedback-1230`.

### 2. Sentry
- **Contrato**: `initSentry()` em main.tsx; DSN em `VITE_SENTRY_DSN`; sample rates `VITE_SENTRY_TRACES_SAMPLE_RATE`, `VITE_SENTRY_REPLAY_SESSION_SAMPLE_RATE`; source maps; `_shared/sentry.ts` nas edge functions.
- **Secrets esperados**: `SENTRY_DSN` (edge functions), `SENTRY_AUTH_TOKEN` (build source maps).

### 3. Vercel (deploy/hosting)
- **Contrato**: vercel.json (framework vite, buildCommand `typecheck && verify_carteira_base44_parity && build`); API serverless `api/health.ts`; Vercel Analytics + Speed Insights (`@vercel/analytics/react`, `@vercel/speed-insights/react`).
- **Secrets esperados**: `VERCEL_GIT_COMMIT_SHA` (injetado), `AUDIT_BASE_URL`, tokens de deploy.
- **Headers/CSP**: vercel.json define CSP completa, HSTS, nosniff, frame-ancestors none.

### 4. Google (Calendar/Drive/Meet/OAuth)
- **Contrato**: edge functions `google-oauth-handler`, `google-calendar-events`, `google-calendar-merged`, `google-calendar-sync`, `google-drive-files`, `google-meet-ata`, `executive-agenda-google-sync`; endpoints `www.googleapis.com`, `oauth2.googleapis.com`, `meet.googleapis.com`, `gmail.googleapis.com`.
- **Secrets esperados**: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`, `GOOGLE_TOKEN_ENCRYPTION_SECRET`, `GOOGLE_CALENDAR_SYNC_ADMIN_TOKEN`, `GMAIL_REFRESH_TOKEN`, `GOOGLE_CENTRAL_REFRESH_TOKEN`, `GOOGLE_CENTRAL_CALENDAR_ID`, `GOOGLE_CENTRAL_CALENDAR_EMAIL`, `GOOGLE_CENTRAL_DRIVE_ROOT_FOLDER_ID`, `GOOGLE_CENTRAL_DRIVE_ROOT_FOLDER_NAME`, `GOOGLE_CALENDAR_ADMIN_MASTER_EMAILS`, `GOOGLE_CALENDAR_PERSONAL_MIRROR_ADMIN_EMAILS`, `GOOGLE_MEET_COHOST_EMAILS`, `GOOGLE_DRIVE_SHARE_EMAILS`.

### 5. OpenRouter (IA)
- **Contrato**: edge function `openrouter-generate`; endpoint `openrouter.ai`; modelos primário/fallback.
- **Secrets esperados**: `OPENROUTER_API_KEY`, `OPENROUTER_PRIMARY_MODEL`, `OPENROUTER_FALLBACK_MODEL`, `OPENROUTER_SITE_URL`, `OPENROUTER_APP_NAME`.

### 6. WhatsApp (links de compartilhamento)
- **Contrato**: links `api.whatsapp.com` em `relatorio-matinal` e `feedback-semanal` (profundidade de link, sem API de mensagens).
- **Secrets**: nenhum.

### 7. Push notifications (Web Push)
- **Contrato**: service worker `public/mx-push-sw.js`; edge function `send-push-notification`; VAPID.
- **Secrets esperados**: `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`; `VITE_VAPID_PUBLIC_KEY` no client.

### 8. Resend (email)
- **Contrato**: `_shared/email.ts`; edge functions de relatório/recuperação de senha.
- **Secrets esperados**: `RESEND_API_KEY`, `RESEND_FROM_EMAIL`.

### 9. GitHub Actions (CI/CD)
- **Contrato**: 21 workflows; code review via `coderabbit-review.yml`.
- **Secrets esperados**: `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_ID`, `SUPABASE_STAGING_URL`, `SUPABASE_STAGING_ANON_KEY`, `SUPABASE_STAGING_TEST_JWT`, `GITLEAKS_LICENSE`, `CODERABBIT_API_KEY`, `GITHUB_TOKEN` (injetado).

## Secrets de cliente (Vite, sem valores)

`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_PUBLIC_APP_URL`, `VITE_APP_URL`, `VITE_APP_VERSION`, `VITE_VAPID_PUBLIC_KEY`, `VITE_SENTRY_DSN`, `VITE_SENTRY_TRACES_SAMPLE_RATE`, `VITE_SENTRY_REPLAY_SESSION_SAMPLE_RATE`, `VITE_MX_ADMIN_MASTER_EMAILS`, `VITE_FLAG_LANCAMENTOS_VIA_RPC`, `VITE_ENABLE_DEV_AUTH_BYPASS`.

## Config de ambiente usada por scripts (server-side, sem valores)

`SUPABASE_SERVICE_ROLE_KEY`, `POSTGRES_URL`, `MX_AUDIT_PASSWORD`, `MX_RESET_PASSWORD`, `MX_RESET_EMAIL`, `MX_E*` (prefixo), `MANAGEMENT_DESIGN_SYSTEM_BASELINE_FILE`, `GOOGLE_TOKEN_ENCRYPTION_SECRET`, `AUDIT_BASE_URL`.

## SDKs consumidos (runtime)

`react-router-dom` (110 refs), `motion/react` (52), `@tanstack/react-query` (15), `zod` (14), `@supabase/supabase-js` (14), `sonner` (3), `@vercel/speed-insights/react`, `@vercel/analytics/react`, `@sentry/react`.

## Achados

| Achado | Classificação |
|---|---|
| 6 famílias de integração: Supabase, Sentry, Vercel, Google, OpenRouter, Web Push/Resend/WhatsApp | OK (mapeado) |
| 12 crons pg_cron ativos (evidência via migrations) | OK |
| CSP completa em vercel.json | OK (defesa em profundidade) |
| `VITE_ENABLE_DEV_AUTH_BYPASS` presente no runtime | RISCO — deve ser false em produção (verificar em Fase 14) |
| 14 secrets Vite client-side + ~20 edge/server | Mapeados sem valores (conforme contrato) |
