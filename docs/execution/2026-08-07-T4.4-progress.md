# T4.4 — Consolidar Tipografia (DONE_WITH_EVIDENCE)

- Data: 2026-08-07
- SHA inicial: `2f0e648f` (HEAD T4.3)
- Status: DONE_WITH_EVIDENCE
- Spec: prompt definitivo §T4.4 (linha 1911)
- Critério de aceite: **Escala única** — OK

## Objetivo

Definir escala única de tipografia: família, sizes, pesos, line-heights,
tracking, títulos (h1-h6), corpo, labels, dados tabulares. Tokens canônicos
em `primitives.css` + `semantic.css` + `@theme`.

## Diagnóstico inicial

### Estado atual
- **Font-family**: hardcoded em 10 lugares do `index.css` (`Inter, system-ui, sans-serif`); `--font-display` no @theme usado em 1 lugar.
- **Font-mono**: 5 mono stacks hardcoded.
- **Dead code no @theme**: `--font-size-mx-nano/micro/tiny/huge` e `--letter-spacing-mx-tight/wide/wider/widest` — declarados mas não consumidos.
- **Font-size hardcoded**: `semantic.css` (14/18/22/28px), `components.css` (14px botão), `internal-mx-manager-scope.css` (múltiplos rem).
- **Consumers**: 281 usos de `text-xs/sm/base/lg/xl` e 322 usos de `font-bold/medium/normal` (classes Tailwind v4 default).
- **Google Fonts**: `@import url('https://fonts.googleapis.com/css2?family=Inter:...')` no topo do index.css.

### Ações executadas
1. ✅ Remover dead code (`--font-size-mx-*`, `--letter-spacing-mx-*`).
2. ✅ Canonizar font-family no @theme (`--font-sans`, `--font-mono`) e remover hardcoded (10 → `var(--mx-font-sans)`, 5 mono → `var(--mx-font-mono)`).
3. ✅ Adicionar primitivos `--mx-font-weight-*`, `--mx-line-height-*`, `--mx-tracking-*` em `primitives.css`.
4. ✅ Mapear via @theme para que Tailwind classes usem os valores canônicos.
5. ✅ Adicionar `@utility` classes para presets semânticos: `text-display`, `text-h1-h6`, `text-body`, `text-body-sm`, `text-caption`, `text-label`, `text-data`.
6. ✅ Doc com escala e padrão (`docs/auditoria/fase-4/T4.4-tipografia.md`).

## Critério "Escala única"

- Família: `Inter` (sans) e `ui-monospace` (mono).
- Size: 12 / 14 / 16 / 18 / 20 / 24 / 30 / 36 / 48 / 60 / 72 px (xs-7xl Tailwind v4).
- Weight: 400 / 500 / 600 / 700 / 800.
- Line-height: 1.25 / 1.375 / 1.5 / 1.625 / 2.
- Tracking: -0.05 / -0.025 / 0 / 0.025 / 0.05 / 0.1 em.

## Checklist (T4.4)

- [x] Primitivos tipográficos em primitives.css (18 tokens)
- [x] Aliases @theme em index.css (18 tokens: `--font-*`, `--leading-*`, `--tracking-*`)
- [x] `--font-display` removido (duplicata morta)
- [x] Dead code `--font-size-mx-*` e `--letter-spacing-mx-*` removido
- [x] 10 `font-family: Inter,...` hardcoded → `var(--mx-font-sans)`
- [x] 5 mono stacks hardcoded → `var(--mx-font-mono)`
- [x] 13 `@utility` presets semânticos em semantic.css (display/h1-h6/body/body-sm/caption/label/data)
- [x] `scripts/audit-typography.mjs` (48 verificações) integrado ao `npm run lint`
- [x] Doc T4.4 + ledger EV-F4-003
- [x] Gates verdes: lint / test / build

## Gates

- `npm run lint` — todos os guards OK (inclui audit-typography 48/48); eslint 0 erros + 1 warning pré-existente HelpTooltip.tsx:35
- `npm test` — 2524 pass / 0 fail / 17945 expects / 451 arquivos
- `npm run build` — ✓ 5.95s

## File list (mudanças desta task)

- `src/design-system/tokens/primitives.css` — seção tipografia (18 tokens)
- `src/design-system/tokens/semantic.css` — 13 @utility presets
- `src/index.css` — @theme aliases, dead code removido, font-family via var(--mx-*)
- `scripts/audit-typography.mjs` — novo guard
- `package.json` — audit-typography adicionado ao lint
- `docs/auditoria/fase-4/T4.4-tipografia.md` — nova doc
- `docs/execution/2026-08-07-T4.4-progress.md` — este arquivo (DONE)
- `docs/execution/2026-08-06-evidence-ledger.md` — EV-F4-003

## Próximo

- **T4.5** Consolidar spacing.
